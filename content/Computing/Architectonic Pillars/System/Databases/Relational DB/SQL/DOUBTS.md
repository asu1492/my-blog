---
---
When to use SCHEMA and when not to use SCHEMA while performing DML operations on table -- e.g. SELECT \* from schema.tableName or SELECT \* FROM tableName; 

* Diff b/w 
	1. membercost < (1/50)\*monthlymaintenance
		
	2. membercost < monthlymaintenance/50.0
